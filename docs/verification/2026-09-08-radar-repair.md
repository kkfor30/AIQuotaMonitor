# 重置雷达故障修复验证（2026-09-08）

基线：8b57542。保留主窗口、设置抽屉、自动分析范围、悬浮页布局。

## 问题与修复

| 问题 | 修复与验证 |
| --- | --- |
| 已标记 v16 却缺少 prepared 表 | v17 增量创建缺失表；已有表不清空；升级前 SQLite online backup 并验证完整性 |
| 旧库缺少状态转换触发器 | v18 独立增量补齐，兼容已经热更新到 v17 的开发库 |
| 旧分析恢复重复加锁 | 释放查询连接后再读取分析；时间声明直接解析，不在事务内另开事务 |
| 旧分析未经输入验证就建事件 | 单帖引用与输入哈希一致才复用；已审计 v20 公告做严格兼容校验；不能验证的正向材料仅排队重试，无关材料不重新消费 |
| 旧公告丢失预计时间 | 唯一有效预告声明绑定时间依据；恢复按原发布时间和实际修复时间判定，已过期限归档 |
| 迟到材料覆盖新时间 | 应用前比较对应事件最新证据时间，旧材料记录为 superseded_evidence，不写当前时间 |
| 卡片增加直接等同公告落地 | 与额度观察共用时间相关条件；检查样本身份、连续性及最长两小时观察间隔；弱相关不抢占事件关联 |
| 长间隔额度恢复被当作精确重置 | 保持 possible_reset，不升级为公告已落地；不调整原恢复幅度阈值 |
| 等待分析同时显示暂无信号 | 摘要明确区分待分析、未检查、来源不可用；失败时详情说明分析失败 |
| 历史分析只取前 100 条 | 单事件分析按时间和 ID 游标分页，界面提供加载更早记录；同时间戳分页回归覆盖 |

## 已执行验证

- desktop typecheck 与 build 通过；cargo check 通过。现存未使用代码及前端分包体积提示不影响编译。
- 全量 Rust 测试 127 项通过；随后补充的同时间戳分析分页回归单独验证。
- 雷达回归包含：缺表旧库升级、事务回滚、旧结果无 prepared 恢复、错误哈希保留重试、迟到时间修订、低相关卡片观察、长快照间隔及双事件隔离。
- 从真实升级前 v16 备份生成副本，冻结北京时间 09/08 08:40 重放：公告 2097043464538264003 首次恢复为 event-f94f4198c01030dd，phase=upcoming，expectedAt=1788832800000（北京时间 09/08 10:00）；重复运行不重复建事件。恢复没有调用模型。
- 开发服务器自动重编译并重启应用，生产数据库已自动升至 v18；prepared 表、状态转换触发器和该公告事件均已只读核实。当前预告时间已过，是否落地仍由有效观察判断。
- 真实网络联调独立在可丢弃副本执行：CodexRadar 10 条、WillCodex 41 条，来源均成功；11 条新增材料经过当前已配置真实模型分析、结构校验和事务应用，活动事件仍为 1 个。此结果不保证外部来源未来永不延迟或模型永不失败。
- 回滚演练：将真实 v16 备份恢复到另一个副本，确认 schema=16、integrity_check=ok、帖子数与备份一致。

## 回滚

1. 先停止应用与 Tauri 开发监视进程，防止自动重启；保留当前数据库的一致性备份。
2. 回退本次提交，恢复升级前 `ai-quota-monitor.v16.bak` 到应用数据目录的 `ai-quota-monitor.db`。应用目录可在设置中查看。
3. 数据库替换必须在所有连接关闭后进行；旧主库、WAL、SHM 应作为同一套数据整体移开，不能混用旧 WAL 与恢复后的备份。
4. 回滚恢复的是修复前状态，也会恢复原先缺表问题；不能仅回退 Git 而保留 v18 数据库。

## 验证边界

本次不声明完整 MVP 端到端验收完成：未重新覆盖 300/420 宽度、四边停靠、深色主题与全部键盘场景，未生成新的实机 UI 截图。界面布局未改，仅更新状态文案和分析分页入口。已保存的旧失败检查日志按历史保留，不伪装成成功检查。
