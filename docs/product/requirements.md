# 产品需求

## 1. 产品目标

AIQuotaMonitor 是一个 Windows 多模型平台统一额度监控中心。用户不需要反复打开各平台官网或 CLI，就能通过桌面主窗口和屏幕边缘悬浮球快速查看额度窗口、Token Plan、余额、消费和异常状态。

首批目标平台：

- GPT / Codex
- Claude Code
- DeepSeek
- GLM
- Kimi
- MiMo
- MiniMax

后续扩展 GPT 重置雷达：抓取 Tibo 等公开内容，结合额度变化、本地规则和可选 AI 分析，推测 GPT 重置可能性。

## 2. 用户价值

- 一个入口观察多个平台的真实额度和余额。
- 在悬浮球中快速判断是否需要切换平台、等待重置或处理登录问题。
- 单个平台或单数据来源失败时，不影响其他数据，也不会把最后成功值错误清零。
- 平台接入方式和数据来源可以替换，不因增删模型重做整个 UI。

## 3. 一级页面

### 总览

- 展示平台总数、正常、部分可用、待配置数量。
- 展示关键窗口和余额摘要、需要关注项、趋势和最近刷新。
- 关注项可直接跳转到平台与具体 Source。
- 不承担凭据配置，不复制平台中心完整详情。

### 平台中心

内部 Tab：

- 额度与用量
- 接入与来源

额度与用量负责查看真实数据、趋势、缓存新鲜度和刷新历史；接入与来源负责 API Key、网页登录、OAuth 或本地 CLI 状态。

平台专属要求：

- DeepSeek：余额、当日/月度消费、模型 Token、请求数、缓存命中/未命中、输出 Token、最近趋势。
- GPT/Codex：窗口额度、Credits、可重置次数和重置信号摘要。
- Claude Code：当前会话和周窗口。
- GLM/Kimi/MiniMax：Token Plan 或 Coding Plan 窗口。
- MiMo：网页登录来源与余额/额度能力。

### GPT 重置雷达

内部 Tab：

- 信号摘要
- Tibo 动态
- 规则与 AI

必须展示结论置信度、证据来源、检查历史和非官方免责声明。Tibo 动态需要原文、翻译、来源、抓取时间、规则命中和额度关联。AI 为可选能力，不能成为唯一决策来源。

### 设置

二级分区：

- 常规
- 外观
- 悬浮球
- 刷新与通知
- 数据与隐私
- 关于

平台登录和 API Key 不属于通用设置，统一在平台中心管理。

## 4. 悬浮球

- 40×40 小球锚点。
- 支持上、下、左、右屏幕边缘和多显示器。
- 支持小球、轻展开、平台速览、平台详情和雷达提醒。
- 支持浅色/深色、智能排序和用户手动排序。
- 全屏应用时自动隐藏，位置可记忆。
- 主窗口和悬浮球共享数据，不重复调用平台接口。

## 5. 平台与 Source 状态

平台聚合状态：

- `healthy`
- `partial`
- `setup_required`
- `error`

数据新鲜度：

- `fresh`
- `stale`
- `missing`

规则：

1. 未配置平台不发请求、不显示示例数据。
2. 一个 Source 失败不取消其他 Source。
3. 有最后成功值时继续展示并标记 stale、最后成功时间和当前错误。
4. 没有真实值时显示 missing，不允许补零。
5. 刷新按 Account/Source 去重，旧请求不能覆盖新账户或新快照。

## 6. 安全与隐私

- 前端 DTO、日志、截图和数据库不得出现完整秘密。
- Web 登录窗口不获得主窗口 Tauri IPC 权限。
- 清除凭据需要确认，并说明会失去的能力。
- 外部请求必须有超时、有限重试、限流和结构化错误。
- 金额用 Decimal；趋势缺失点与真实零值必须区分。

## 7. 非目标

MVP 不做：

- 通用 AI API 代理或自动路由。
- MCP/Skills 管理。
- 团队多租户、云同步或远程 Web 管理。
- 任意 JavaScript 用量脚本。
- 用设计稿示例数字替代真实后端数据。

## 8. 设计资产

- `docs/ui-design/platform-center-v4/`
- `docs/ui-design/2026-08-27-product-shell-v5/`

每个目录的 `README.md` 包含页面职责、交互和数据边界。
